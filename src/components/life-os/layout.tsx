"use client";
import { cn } from "@/lib/utils";
import { Icon } from "./icon";
import { Button } from "@/components/ui/button";
export function PageHeader({ title, subtitle, icon, color, actions }: { title: string; subtitle?: string; icon?: string; color?: string; actions?: React.ReactNode }) { return <div className="flex flex-wrap items-end justify-between gap-4"><div className="flex items-center gap-3">{icon && <span className="flex h-10 w-10 items-center justify-center rounded-md border border-zinc-800 bg-zinc-900 font-mono" style={{ color: color || "var(--muted-foreground)" }}><Icon name={icon} className="h-5 w-5" /></span>}<div><h1 className="font-mono text-2xl font-semibold tracking-tight">{title}</h1>{subtitle && <p className="mt-1 font-mono text-xs text-muted-foreground">{subtitle}</p>}</div></div>{actions && <div className="flex items-center gap-2">{actions}</div>}</div>; }
export function SectionCard({ title, icon, action, children, className, bodyClassName }: { title?: string; icon?: string; action?: React.ReactNode; children: React.ReactNode; className?: string; bodyClassName?: string }) { return <section className={cn("rounded-md border border-zinc-800 bg-card/40 p-4", className)}>{title && <div className="mb-3 flex items-center justify-between"><h3 className="flex items-center gap-2 font-mono text-xs font-semibold uppercase tracking-wide">{icon && <Icon name={icon} className="h-4 w-4 text-muted-foreground" />}{title}</h3>{action}</div>}<div className={bodyClassName}>{children}</div></section>; }
export function EmptyState({ icon = "Circle", title, description, action }: { icon?: string; title: string; description?: string; action?: { label: string; onClick: () => void } }) { return <div className="flex flex-col items-center justify-center rounded-md border border-dashed border-zinc-800 py-16 text-center"><div className="mb-3 flex h-12 w-12 items-center justify-center rounded-md border border-zinc-800 bg-zinc-900"><Icon name={icon} className="h-6 w-6 text-zinc-500" /></div><h3 className="font-mono text-sm font-semibold">{title}</h3>{description && <p className="mt-1 max-w-xs text-xs text-muted-foreground">{description}</p>}{action && <Button size="sm" className="mt-4 rounded-md bg-zinc-100 font-mono text-xs text-zinc-950 hover:bg-white" onClick={action.onClick}>{action.label}</Button>}</div>; }
export function StatPill({ label, value, color, icon }: { label: string; value: string | number; color?: string; icon?: string }) { return <div className="flex items-center gap-2.5 rounded-md border border-zinc-800 bg-card/40 px-3 py-2.5">{icon && <span className="flex h-8 w-8 items-center justify-center rounded-md border border-zinc-800 bg-zinc-900" style={{ color: color || "var(--muted-foreground)" }}><Icon name={icon} className="h-4 w-4" /></span>}<div><div className="font-mono text-lg font-semibold leading-none">{value}</div><div className="mt-0.5 text-[11px] text-muted-foreground">{label}</div></div></div>; }
